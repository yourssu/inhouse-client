import { vars as interiorVars } from '@yourssu-inhouse/interior-vars';

/**
 * @deprecated 내부적으로 @yourssu-inhouse/interior-vars 로 마이그레이션 중이에요.
 * 사용처는 `import { vars } from '@yourssu-inhouse/interior-vars'` (`vars.color.*`) 로
 * 옮겨지며, 이 파일은 제거될 예정이에요.
 */
export const vars = interiorVars.color;
