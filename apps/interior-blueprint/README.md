# interior-blueprint

interior 디자인 시스템 showcase 앱. Module Federation remote 가 아니라 standalone Vite 앱이에요.

```bash
pnpm --filter @yourssu-inhouse/interior-blueprint dev
```

개발 서버는 포트 `6100` 에 떠요. (6000~6063 은 브라우저가 unsafe port 로 차단해 쓸 수 없어요.)
